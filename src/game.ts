/* It's importing the `createCryptoChess` function from the `main.ts` file in the `scenes/CryptoChess`
directory. */
import { createCryptoChess } from "scenes/CryptoChess/main"

/* It's creating the `CryptoChess` scene. */
createCryptoChess()